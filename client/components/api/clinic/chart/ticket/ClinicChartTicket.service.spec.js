'use strict';

describe('Service: ClinicChartTicket', function () {

  // load the service's module
  beforeEach(module('petz.api'));

  // instantiate service
  var sut;
  beforeEach(inject(function (_ClinicChartTicket_) {
    sut = _ClinicChartTicket_;
  }));

  it('should do something', function () {
  });

});
