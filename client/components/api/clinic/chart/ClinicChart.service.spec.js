'use strict';

describe('Service: ClinicChart', function () {

  // load the service's module
  beforeEach(module('petz.api'));

  // instantiate service
  var sut;
  beforeEach(inject(function (_ClinicChart_) {
    sut = _ClinicChart_;
  }));

  it('should do something', function () {
  });

});
