'use strict';

describe('Service: ClinicTicket', function () {

  // load the service's module
  beforeEach(module('petz.api'));

  // instantiate service
  var sut;
  beforeEach(inject(function (_ClinicTicket_) {
    sut = _ClinicTicket_;
  }));

  it('should do something', function () {
  });

});
